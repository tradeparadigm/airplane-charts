apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubernetes_agent.fullname" . }}-test-connection"
  labels:
    {{- include "kubernetes_agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubernetes_agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
